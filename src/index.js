import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';




ReactDOM.render(
    <App />, // component, react는 하나의 component만 가져올 수 있음
  document.getElementById('root') //root에 요소를 넣어줌
);


